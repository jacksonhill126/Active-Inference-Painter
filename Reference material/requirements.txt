# Runtime-only dependency list kept for legacy `pip install -r` users.
# The authoritative source is `pyproject.toml`. Prefer:
#
#     uv sync                    # full dev environment (recommended)
#     uv pip install -e .        # editable install of the runtime deps only
#     pip install -e ".[dev]"    # pip fallback
#
numpy>=1.23
scipy>=1.10
matplotlib>=3.6
pillow>=10.0
