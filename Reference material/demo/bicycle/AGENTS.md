# `demo/bicycle/` — agent guide

| Script | APIs | Chapters |
| --- | --- | --- |
| `visualize_bicycle.py` | `build_bicycle_demo` → multivariate AIF + fault trace | 7, 13 |

Stochastic workflow uses `--seed` in library metadata (`seed=7` default).
