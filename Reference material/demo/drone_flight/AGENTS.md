# `demo/drone_flight/` — agent guide

| Script | APIs | Chapters |
| --- | --- | --- |
| `visualize_drone_flight.py` | `build_drone_flight_demo` → POMDP + navigation + LGS | 3, 9, 13 |

Stochastic noisy reads use `--seed` in library metadata (`seed=11` default).
