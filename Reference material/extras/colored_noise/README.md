# Colored Noise

Book sections: Appendix C.9.

Run:

```bash
uv run python extras/colored_noise/visualize_colored_noise.py --save
```

Saved artifacts are written to `output/figures/extras/colored_noise` and
`output/data/extras/colored_noise`.
