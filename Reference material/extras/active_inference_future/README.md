# Active Inference Future

Book sections: Appendix A.2 and §A.2.5.

Run:

```bash
uv run python extras/active_inference_future/visualize_active_inference_future.py --save
```

Saved artifacts are written to `output/figures/extras/active_inference_future`
and `output/data/extras/active_inference_future`.
