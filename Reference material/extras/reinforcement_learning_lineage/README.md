# Reinforcement Learning Lineage

Book sections: Appendix A.2.4.

Run:

```bash
uv run python extras/reinforcement_learning_lineage/visualize_reinforcement_learning_lineage.py --save
```

Saved artifacts are written to
`output/figures/extras/reinforcement_learning_lineage` and
`output/data/extras/reinforcement_learning_lineage`.
