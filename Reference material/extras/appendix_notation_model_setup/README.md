# Notation And Model Setup

Book sections: Appendix B.1-B.12.

Run:

```bash
uv run python extras/appendix_notation_model_setup/visualize_appendix_notation_model_setup.py --save
```

Saved artifacts are written to `output/figures/extras/appendix_notation_model_setup`
and `output/data/extras/appendix_notation_model_setup`.
